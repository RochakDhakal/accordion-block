import React from "react";

function Edit({ attributes, setAttributes }) {
	const { toggleAccordion, accordionHeading, accordionContent } = attributes;
	return (
		<>
			<div>
                
            </div>
		</>
	);
}

export default Edit;
